const {Router} = require('express')
const router = Router()

router.get('/', (request, esponse) => {})

router.post('/', (request, esponse) => {})

router.put('/:id', (request, esponse) => {})

router.delete('/:id', (request, esponse) => {})

module.exports = router
